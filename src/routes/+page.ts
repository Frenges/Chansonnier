/**
 * Root landing page route.
 *
 * The home page is intentionally simple and renders immediately in the
 * client shell. It relies on the shared root layout data rather than
 * defining a separate page-level loader.
 */
export const ssr = false;
